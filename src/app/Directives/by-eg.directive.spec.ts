import { ByEgDirective } from './by-eg.directive';

describe('ByEgDirective', () => {
  it('should create an instance', () => {
    const directive = new ByEgDirective();
    expect(directive).toBeTruthy();
  });
});
